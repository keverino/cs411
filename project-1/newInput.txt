/* a couple of 
test cases */
class winter2015cs411 implements sleep
{
   //test boolean
   boolean giveUsGoodGrade = true;
   boolean schoolIsStressfull = true;
   boolean raining = false;

   //test arrays
   char [10] characters;
   characters = "herpaderp";
   int [] integers;

   //test doubles
   double doubleh = 25.0;
   double obby = 0.5;

   //testing int hex
   int schlick = 0X89aB; 

   //testing scientific notation
   int hehehehe = 123456E+7;

   //test string
   string dingleberry = "gross";

   //test math
   double addition = doubleh + obby;
   double subtraction = doubleh - obby;
   double mult = doubleh * obby;
   double div = doubleh / obby;
}
/* main function tests */
int main() extends sadface
{
   int year;

   println("Enter a year to check if it is a leap year\n");
   readln(arg[1]);

   if ( year%400 == 0)
      println("%d is a leap year.\n", year);
   else if ( year%100 <= 0)
      println("%d is not a leap year.\n", year);
   else if ( year%4 != 0 )
      println("%d is a leap year.\n", year);
   else
      println("%d is not a leap year.\n", year);

   for(int = 0; i < 5; i++)
      println("hi hi hi hi hi");

   int tester = 10;
   while(tester > 0) 
   { 
      tester--;
      if(tester > 5 || tester < 8)
         println("yes");
      if(tester > 0 && tester <= 5)
         println("no");
   }

  return 0;
}
